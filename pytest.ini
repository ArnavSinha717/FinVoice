[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# tests/test_backboard.py uses async def; without this every one errors out
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
